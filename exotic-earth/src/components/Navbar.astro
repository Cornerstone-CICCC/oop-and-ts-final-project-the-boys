<header class="top-nav">
  <div class="brand">
    <div class="brand-icon">▦</div>
    <span class="brand-name">KanbanFlow</span>
  </div>

  <nav class="menu-links" aria-label="Primary">
    <a href="#" class="active">Dashboard</a>
    <a href="#">Boards</a>
    <a href="#">Teams</a>
    <a href="#">Reports</a>
  </nav>

  <div class="search-wrap">
    <div class="search-box">
      <span class="search-icon">⌕</span>
      <input type="text" aria-label="Search" />
    </div>
  </div>

  <button type="button" class="add-task">+ Add Task</button>
  <button type="button" class="profile" aria-label="Profile"></button>
</header>

<style>
  .top-nav {
    position: sticky;
    top: 0;
    z-index: 10;
    height: 72px;
    padding: 0 22px;
    display: grid;
    grid-template-columns: auto auto minmax(240px, 1fr) auto auto;
    align-items: center;
    gap: 18px;
    border-bottom: 1px solid #1a2943;
    background: #09172b;
  }

  .brand {
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .brand-icon {
    width: 30px;
    height: 30px;
    border-radius: 8px;
    display: grid;
    place-items: center;
    font-size: 12px;
    color: #dff0ff;
    background: #1f67de;
  }

  .brand-name {
    font-size: 1.05rem;
    font-weight: 600;
    color: #f0f6ff;
    letter-spacing: 0.2px;
  }

  .menu-links {
    display: flex;
    align-items: center;
    gap: 18px;
  }

  .menu-links a {
    text-decoration: none;
    font-size: 0.9rem;
    color: #8ca2c2;
  }

  .menu-links a.active {
    color: #56a9ff;
  }

  .search-wrap {
    position: relative;
    justify-self: end;
    width: 100%;
    max-width: 330px;
  }

  .search-box {
    height: 44px;
    border-radius: 10px;
    border: 1px solid #1c2d49;
    background: #11203a;
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 0 12px;
  }

  .search-icon {
    color: #6f88ad;
    font-size: 14px;
  }

  .search-box input {
    width: 100%;
    border: 0;
    outline: none;
    background: transparent;
    color: #dce9ff;
    font-size: 0.92rem;
    text-transform: lowercase;
  }

  .add-task {
    height: 44px;
    border: 0;
    border-radius: 10px;
    padding: 0 18px;
    font-weight: 600;
    color: #eef5ff;
    background: #1f67de;
  }

  .profile {
    width: 38px;
    height: 38px;
    border-radius: 50%;
    border: 2px solid #122744;
    background: #e7dbc8;
  }

  @media (max-width: 980px) {
    .top-nav {
      grid-template-columns: auto 1fr auto;
      grid-template-areas:
        'brand search profile'
        'links links add';
      height: auto;
      padding: 12px 16px;
      row-gap: 10px;
    }

    .brand {
      grid-area: brand;
    }

    .menu-links {
      grid-area: links;
      flex-wrap: wrap;
      gap: 12px;
    }

    .search-wrap {
      grid-area: search;
      justify-self: stretch;
      max-width: none;
    }

    .add-task {
      grid-area: add;
      justify-self: end;
    }

    .profile {
      grid-area: profile;
      justify-self: end;
    }
  }
</style>
